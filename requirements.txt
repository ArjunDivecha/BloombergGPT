fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
slowapi==0.1.9  # For rate limiting
pandas==2.2.2

# Bloomberg API (blpapi) must be installed separately from Bloomberg.
# Download the wheel from Bloomberg's API portal and install via:
#   pip install path/to/blpapi-<version>.whl
